cmake_minimum_required(VERSION 3.1)

project(HDAC_SCRIPT
    DESCRIPTION "the function related to the script in transation")

set(PROJECT_VERSION_MAJOR 0)
set(PROJECT_VERSION_MINOR 1)
set(PROJECT_VERSION_PATCH 0)

add_subdirectory(src)
